#pragma once
#include "QuadTree.h"

int main()
{
   //QuadTree quadTree;

   //quadTree.Divide();
   //quadTree.GetChild(0).Divide();
   //quadTree.GetChild(0).GetChild(3).Divide();
   //quadTree.GetChild(1).Divide();
   ////quadTree.GetChild(2).Divide();
   ////quadTree.GetChild(3).Divide();

   //auto& target = quadTree.GetChild(0).GetChild(2);
   //auto lodDiff = target.CalculateLevelDifference(Direction::Down);

   ////TODO: handle edges of quads and nulls.

   //QuadTree* tree;
   //for(auto& index : downIndex)
   //{
   //   quadTree.GetChild(index);
   //}


   return 0;
}