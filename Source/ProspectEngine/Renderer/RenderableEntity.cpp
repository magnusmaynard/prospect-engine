#include "RenderableEntity.h"
#include <memory>

using namespace Prospect;

RenderableEntity::RenderableEntity(Entity_impl& entity)
   :
   m_entity(entity)
{
   //Compile

   //Get uniform bindings

   //Construct VAO.


   //m_program.AddVertexShader("generic");
   //m_program.AddFragmentShader("generic");
}
